<p>
  <strong>Title:</strong>
  <%= @advertising.title %>
</p>

<p>
  <strong>Text:</strong>
  <%= @advertising.text %>
</p>

<% if @advertising.images.attached? %>
<p>
  <strong>Images:</strong>
  <br>
  <% @advertising.images.each do |image| %>
    <%= image_tag(image) %>
  <% end %>
</p>
<% end %>

<%= link_to 'Edit', edit_advertising_path(@advertising) %>
<%= link_to 'Back', advertisings_path %>
