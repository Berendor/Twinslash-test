<%= form_with model: @advertising, local: true do |form| %>

  <% if @advertising.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@advertising.errors.count, "error") %> prohibited
        this advertising from being saved:
      </h2>
      <ul>
        <% @advertising.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
  </p>

  <p>
    <%= form.label :text %><br>
    <%= form.text_area :text %>
  </p>

  <p>
    <%= form.submit %>
  </p>

<% end %>
